[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hermes-mobile-plugin"
version = "0.0.11"
description = "Hermes Mobile QR Plugin - Generates QR code for direct 8642 connection"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Sachin Taware"}]
dependencies = [
    "pyyaml>=6.0",
    "qrcode>=7.4",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "black",
    "ruff",
]

[project.scripts]
hermes-mobile-plugin = "hermes_mobile_plugin.cli:main"
hermes-mobile-install = "hermes_mobile_plugin.cli:install_plugin"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
# Ship the loader manifest inside the wheel so `hermes-mobile-install`
# works from a pip install (no repo checkout) — the deploy code looks for
# plugin.yaml beside the package when the repo root is not there.
hermes_mobile_plugin = ["plugin.yaml"]

[tool.black]
line-length = 88
target-version = ["py310"]

[tool.ruff]
line-length = 88
target-version = "py310"